[English](PRIVACY.md) · [Tiếng Việt](PRIVACY.vi.md) · [中文](PRIVACY.zh.md) · **日本語**

# Deskhub プライバシーポリシー

_発効日：2026 年 8 月 28 日 — バージョン 2.4_

> 本書は [`PRIVACY.md`](PRIVACY.md) の翻訳である。相違がある場合は**英語版が法的効力を
> 持つ版**となる。

## 1. はじめに

本プライバシーポリシーは、**Deskhub**（「本アプリ」「私たち」）が、Deskhub のモバイル
アプリ（iOS、Android）および Windows・macOS・Linux 向けの Deskhub デスクトップアプリ
（あわせて「本ソフトウェア」）の利用にあたって情報をどう扱うかを説明する。

Deskhub はリモートデスクトップアプリである。あなたのコンピュータの画面をあなたの別の
端末へ流し、マウス、キーボード、タッチ入力でそのコンピュータを操作させる。

本ソフトウェアは個人開発者が開発・公開している：

- **開発者：** Manh Pham
- **連絡先：** manhpv151090@gmail.com
- **プロジェクトページ：** https://github.com/manhpham90vn/Deskhub

## 2. 短い版

**Deskhub は個人データを収集も、保存も、販売も、共有もしない。私たちはサーバを一切
運用しておらず、あなたや利用状況に関するデータが本ソフトウェアを通じて私たちや第三者に
届くことはない。** ユーザーアカウントも、アナリティクスも、クラッシュレポートも、広告も
なく、本ソフトウェアに third-party SDK は組み込まれていない。

## 3. 本ソフトウェアが処理する情報

動作のために、本ソフトウェアは一部のデータを**完全にあなた自身の端末の中と端末間だけで**
処理しなければならない。そのいずれも開発者や第三者に送られることはない。

| データ | 目的 | 行き先 | 保持 |
|---|---|---|---|
| 共有されているコンピュータの画面内容（映像フレーム） | その画面をあなたの別の端末に表示するため | あなたの 2 台の間で直接送られ、転送中は暗号化される（QUIC/TLS） | 保存しない。セッション中メモリ上にのみ存在 |
| 共有されているコンピュータが鳴らしている音（音を共有していて、ビューアが求めた場合のみ） | 見ている人がそのコンピュータの音を聞けるようにするため | あなたの 2 台の間で圧縮音声として直接送られ、転送中は暗号化される（QUIC/TLS） | 保存しない。セッション中メモリ上にのみ存在 |
| マウス、キーボード、タッチの入力 | 別の端末から共有中のコンピュータを操作するため | 視聴側の端末から共有中のコンピュータへ直接送られ、転送中は暗号化される（QUIC/TLS） | 保存しない。注入後に破棄 |
| このマシンの鍵ペア — 初回起動時に作られる秘密鍵と自己署名証明書 | 接続先のマシンにこのマシンの身元を証明するため。人にはフィンガープリント（`SHA256:…`）として見える | アプリ専用フォルダの `host_key.pem` と `host_cert.pem` に書かれる。接続先に提示されるのは公開側（証明書）だけ | ファイルを削除するまで保持。削除するとマシンは新しい身元になり、古い身元を知るマシンは警告する |
| この端末が信頼したホストの鍵（フィンガープリント、アドレス、ラベル、初回/最終確認時刻） | 既知のホストを認識し、鍵が変わったら大きく警告するため | 同じフォルダの `known_hosts` に書かれる。送信されることはない | ファイルを削除するまで保持 |
| このホストとペアリングしたマシン — 鍵のフィンガープリント、送ってきた名前、ペアリング時刻と最終確認時刻 | ペアリング済みのマシンがパスコードなしで再接続できるようにし、Devices 画面に並べて忘れさせられるようにするため | 同じフォルダの `paired_devices` に書かれる。送信されることはない | Devices 画面でそのマシンを忘れるか、ファイルを削除するまで保持 |
| パスコード検証子のためのランダムなソルト | パスコードを、ペアリングのハンドシェイクが照合する値に変えるため。これによりコード自体が流れない | 同じフォルダの `auth_salt` に書かれる。ハンドシェイク中に接続してきたマシンへ送られる（秘密ではない） | ファイルを削除するまで保持 |
| あなたが入力するアドレス（IP/ホスト名） | 相手のマシンに接続するため | 入力した端末に留まる | 変更するまでローカルに保持 |
| 直近 10 件の接続先アドレス、それぞれの時刻、それぞれに使ったパスコード | *最近の端末* 一覧を埋めて、ワンクリックで再接続できるようにするため | 端末上のアプリ専用フォルダの `recent-devices.txt` に書かれる — Windows では `%USERPROFILE%\.deskhub`、macOS と Linux では `~/.deskhub`、iOS と Android ではアプリのサンドボックス | 新しいアドレスに 10 回接続するか、ファイルを削除するまで保持 |
| 共有の設定（フレームレート、ビットレート、解像度上限、ポート、どのネットワークアドレスで共有するか、ビューアがマシンを操作してよいか、クリップボード同期・スリープ防止・OS 起動時に起動・自動共有・バックグラウンドモードの各トグル、ビューアに要求するパスコード） | 次にアプリを開いたときに設定を復元するため | 同じフォルダの `ui-settings.txt` に書かれる。iOS ではアプリとブロードキャスト拡張が共有する app group コンテナに置かれ、両者がパスコードとポートについて一致するようにしている | 変更するかファイルを削除するまで保持 |
| Linux デスクトップが画面共有ダイアログでディスプレイを選んだ後に発行する画面権限トークン（Linux のみ） | 以後の共有が選択を黙って再利用し、ダイアログが初回だけ出るようにするため | 同じフォルダの `portal-restore-token.txt` に書かれる。トークンはこのマシンのあなた自身のデスクトップセッションにしか意味がなく、送信されることはない | 共有のたびに置き換えられ、*Choose screens again* を押すかファイルを削除すると消える |
| クリップボードのテキスト（クリップボード同期がオンで、セッションが動いている間だけ） | ある端末でコピーしたテキストを他の端末で貼り付けられるようにするため | あなたの端末間で直接送られ、転送中は暗号化される（QUIC/TLS）。1 回のコピーにつき 32 KiB まで。プレーンテキストのみで、画像やファイルは決して送らない | Deskhub は保存しない。各端末の通常のシステムクリップボードにのみ存在 |
| 現在ブロードキャストが動いているか、接続中のビューア数、ブロードキャスト拡張自身のメモリ使用量（MB）、直近の起動エラーの文言（iOS のみ） | アプリの共有画面がブロードキャスト拡張の状態を表示できるようにするため。iOS はそれを別プロセスとして動かし、メモリを使いすぎると終了させる | 同じ app group コンテナの `broadcast-status.txt` に書かれる | ブロードキャスト終了時に削除 |
| *Your name* 欄のデバイス名 — 編集するまでは、このコンピュータや端末自身の名前（Windows と Linux ではホスト名、macOS ではコンピュータ名、iOS では端末名、Android ではモデル名）が入っている | 接続先のホストで、この端末のアドレスの隣に表示され、共有している人がビューアを見分けられるようにするため | 同じフォルダの `ui-settings.txt` に保存され、接続時にホストへ送られる — 転送中は暗号化されるが、ホストの画面に表示され、そのログにも書かれる — したがって自分で選んだ名前に置き換えない限り、この既定の名前が送信される。欄を空にしても既定値が戻るだけで、それが保存され送られる。マシンがペアリングすると、ホストはその名前を、そこで忘れられるまで `paired_devices` 一覧にも保存する | 既定はコンピュータや端末の名前。変更するかファイルを削除するまで保持。欄を空にすると名前が消えるのではなく既定値に戻る |
| 接続先のコンピュータへ送ることを選んだファイル（自分で選んで Send を押したときだけ） | 自分の端末から別の端末へファイルを置くため | あなたの 2 台の間で直接送られ、転送中は暗号化される（QUIC/TLS）。スマートフォンやタブレットでは、送信中に読めるようアプリ自身のキャッシュにコピーがまず置かれる | ファイルの行き先は受け取る側による。コンピュータはそのために選んだフォルダ — 別途選ばない限りそのユーザーのホームフォルダの `Deskhub` — に書き、そのユーザーが削除するまで保持する。スマートフォンやタブレットにはそのようなフォルダがない：写真や動画はその端末の写真ライブラリに追加され（Android では `Pictures/Deskhub` と `Movies/Deskhub`）、それ以外はシステムのファイルブラウザから見える場所 — iOS ではアプリの Documents、Android では `Download/Deskhub` — に置かれ、削除するまでそこに残る。iOS ではライブラリが拒んだ写真は代わりに Documents へ行く。送信側のスマートフォンやタブレットに置かれたコピーは、送信ウィンドウを閉じたときに削除される |
| 申し出た各ファイルの名前・サイズ・チェックサム、および送信側端末の名前・アドレス・鍵フィンガープリント | 受け取るコンピュータが何が届くかを表示し、保存できないものを拒めるようにし、その持ち主が誰が何を送ったかを見られるようにするため | あなたの 2 台の間で送られ、転送中は暗号化される。受け取るコンピュータは申し出、その判断、結果を自身のセッションログに書く | そのコンピュータのログファイルに、削除するまで保持 |
| コンピュータが受け取ったファイルを保存するフォルダ | 次にアプリを開いたときに選択を復元するため | アプリ専用フォルダの `ui-settings.txt` に書かれる。送信されることはない | 変更するかファイルを削除するまで保持 |
| 接続の統計（ビットレート、パケット損失、遅延） | ストリームの品質を適応させるため。ステータスバーに表示される | あなたの 2 台の間でのみやり取りされる | 保存しない。セッション終了時に破棄 |

### 3.1 設計からしてピアツーピア

すべての通信は**あなた自身の 2 台の端末の間で直接**行われる。経路は：

- あなたのローカルネットワーク（Wi-Fi/LAN）、または
- インターネット越しのアクセスに使うことを選んだ場合、**あなた自身が**運用または契約
  している VPN（たとえば Tailscale）。

私たちはリレーサーバもシグナリングサーバも、その他のバックエンドも運用していない。本
ソフトウェアには開発者へデータを送る技術的手段が存在しない。

### 3.2 私たちが処理**しない**データ

本ソフトウェアは次にアクセスも処理もしない：あなたの氏名（上記のデバイス名は除く。既定は
コンピュータや端末自身の名前）、メールアドレス、電話番号、連絡先、位置情報、写真、
ファイル（あなたが流すことを選んだ PC 画面に映るものを除く）、マイク、カメラ、広告
識別子、およびアプリを動かすために OS が必要とする以上の端末識別子。

### 3.3 スマートフォンやタブレットの画面を共有する

Android と iOS の端末は、他のマシンを見るだけでなく自分の画面を共有することもできる。
そのストリームは**閲覧専用**である：届いたマウスとキーボードのパケットは捨てられる。
どちらの OS も普通のアプリに端末を操作させないからである。取り込まれるのは**画面全体**で
あり、共有中に現れるものすべて — 通知、他のアプリ、銀行アプリ、入力するパスワード — が
含まれる。Android では共有のたびにシステム自身の録画同意ダイアログが出て、動作中は常設の
通知が表示される。iOS ではシステムのブロードキャストインジケータが出続ける。どちらも OS
自身のシグナルであり、どちらからでもいつでも共有を止められる。デスクトップと同じく、映像は
あなたの別の端末へ直接届き、保存されることも私たちに送られることもない。

### 3.4 画面共有と遠隔操作の範囲

共有は**選択したディスプレイ全体**を流す。そのモニタに映るものはすべて、接続中のビューアに
見える。通知、ポップアップ、共有中に開いたウィンドウも含む。（単一アプリのウィンドウを
共有する機能は 2026-07-27 に削除された。本ソフトウェアは現在ディスプレイ単位でのみ共有
する。）遠隔操作を許すと、ビューアの入力は PC の前に座っているかのように注入され、
**共有中のディスプレイに見えるどのアプリにも**届く — もはやひとつのウィンドウに限られない。
どのホストでも設定から遠隔操作を完全に切ることができ、そうすると共有は閲覧専用になる：
届いた入力は注入されず捨てられる。操作が許されている間は 2 つの安全機構が常に働く。PC の
前の人が実物のマウスかキーボードに触れればリモート入力は一時停止し（「ホストが勝つ」）、
接続が終わるかビューアが離れると、リモート側が押しているキーは自動的に解放される。1 台の
PC を同時に見られるのは最大 5 人だが、ある瞬間にマウスとキーボードを操作するのはそのうち
1 人だけである。

## 4. アプリが要求する権限

| プラットフォーム | 権限 | 理由 |
|---|---|---|
| iOS | ローカルネットワーク | 同じネットワーク上の PC と通信するために iOS が要求する。ストリーミングのセッションにのみ使う。 |
| iOS | 画面収録（ブロードキャスト） | この端末の画面共有をシステムのブロードキャストピッカーから開始するときのみ。iOS は毎回尋ね、その間ずっと録画インジケータを表示する。 |
| Android | `INTERNET`、ネットワーク状態 | PC への UDP 接続を開くために必要。ストリーミングのセッションにのみ使う。 |
| Android | 画面キャプチャの同意（`MediaProjection`） | この端末の画面共有を開始するときのみ。Android は毎回尋ね、その答えは記憶できない。 |
| Android | `FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_MEDIA_PROJECTION` | アプリがバックグラウンドにあるときや画面が消えているときも共有を続けるため。画面キャプチャに Android が要求する。 |
| Android | `RECORD_AUDIO` | Android は再生キャプチャ API をこの権限の後ろに置いており、Deskhub が取り込むのは再生 — 端末自身が鳴らしている音 — だけである。共有開始時に求め、拒否すれば音なしで共有が進む。Deskhub がマイクを開くことは決してない。 |
| Android | `POST_NOTIFICATIONS` | 画面共有中に Android が要求する継続通知を表示し、別の端末からファイルが届いたときにその名前を知らせるため。それ以外の通知は送らない。 |
| iOS | 写真ライブラリ、追加のみ | 誰かが送った写真や動画がこの端末に初めて届いたときに求め、写真アプリに入れられるようにする。Deskhub は追加しかできない：ライブラリにあるものを読むことも、変えることも、消すこともない。拒否すればファイルはアプリの Documents フォルダへ行く。 |
| iOS | 通知 | 別の端末からファイルが届いたときにその名前を知らせるため。それ以外の通知は送らない。 |

デスクトップで音を共有するのに専用の権限は要らない。取り込むのはコンピュータ自身が鳴らして
いる音であって、マイクではない。Android だけが例外で、それも名前の上だけである：再生
キャプチャ API が `RECORD_AUDIO` の後ろにあるため、音を共有する Android 端末はシステムが
*マイク*と表示する権限を持たねばならない。Deskhub はそれを他の用途に使わない。マイクを
録音することは決してなく、双方向音声もなく、他のどのプラットフォームでもマイク権限を
求めない。

これらのアプリは他の権限を要求しない。将来のバージョンが新しい権限を必要とする場合は、
その文脈の中で要求し、本ポリシーを更新する。

## 5. アナリティクス、広告、第三者

- **アナリティクス / テレメトリ：** なし。
- **クラッシュレポート：** なし。診断ログ（`[DIAG]`）はあなた自身のマシンにしか存在しない
  — アプリのコンソール出力と、Windows・macOS・Linux では `~/.deskhub/`
  （Windows では `%USERPROFILE%\.deskhub`）配下の平文ファイルの中である。どこにも
  アップロードされない。あなた自身がコピーして送らない限り端末を離れず、そのフォルダは
  いつでも削除できる。
- **広告：** なし。
- **third-party SDK：** なし。本ソフトウェアは自身のソースコード（プロジェクトページで
  入手できる）と OS のフレームワークだけから作られている。
- **アプリストア：** アプリは Apple App Store と Google Play で配布している。Apple と
  Google はそれぞれのプライバシーポリシーの下でインストールや利用の統計を集めることが
  あるが、その収集は私たちの管理外であり、私たちが受け取るのはそれらのプラットフォームが
  すべての開発者に見せる集計済み・匿名の統計だけである。
- **Tailscale などの VPN：** VPN 経由で接続することを選んだ場合、通信はその提供者の
  プライバシーポリシーの下で扱われる。Deskhub は VPN を必須とせず、同梱もしない。

## 6. セキュリティ

- ストリーミングの通信はあなた自身のネットワークか、あなた自身の VPN トンネルの中に
  留まる。Tailscale のような VPN を使う場合、端末間の通信はその VPN（WireGuard）に
  よってエンドツーエンドに暗号化される。
- Deskhub はセッションの通信を暗号化する — 映像、制御、入力、クリップボード、ターミナルの
  データはすべて、端末間の QUIC/TLS の上を流れる。入場はペアリングのハンドシェイクで
  決まる：未知のマシンは、ホストの任意の 4 桁パスコードを知っていると証明する（コード自体は
  決して送信されない）か、ホストの前の人に承認されねばならない。デバイス名は転送中こそ
  暗号化されるがホストに表示されるので、機微なものは入れないこと。Deskhub をインターネットに
  直接さらしてはならない。完全な脅威モデル — 何が守られ、何が守られず、脆弱性をどう報告
  するか — は
  [`SECURITY.md`](https://github.com/manhpham90vn/Deskhub/blob/main/SECURITY.md) にある。
- `recent-devices.txt` と `ui-settings.txt` に保存されるパスコードは固定鍵で難読化されて
  おり、ひと目では読めない。それは暗号化ではなく、すでにあなたのユーザーアカウントに
  アクセスできる者を防ぐためのものでもない。
- 私たちがあなたのデータを一切持たない以上、侵害されうる開発者側のデータベースも存在
  しない。

## 7. データの保持と削除

私たちは何も保持しないので、私たちが削除するものもない。セッションのデータはセッションが
終われば消える。アプリに保存されたアドレスは、欄を消すかアプリを削除すれば無くなる。
最近の端末一覧と保存された設定 — パスコードを含む — は、アプリのフォルダ（Windows では
`%USERPROFILE%\.deskhub`、macOS と Linux では `~/.deskhub`）を削除すれば無くなり、アプリは
次の起動時に空のものを作り直す。iOS と Android ではアプリを削除すれば無くなる。

別の端末があなたに送ったファイルはあなたのものであって、アプリのものではない。届いた後は
そのコンピュータが選んだフォルダ、あるいはスマートフォンやタブレットの写真ライブラリ・
Documents・Downloads にあり、Deskhub を削除してもそのまま残る。削除はそこで行うこと。

## 8. あなたの権利（GDPR、CCPA、および類似の法律）

EU 一般データ保護規則（GDPR）やカリフォルニア州消費者プライバシー法（CCPA）などの法律は、
個人データに対する権利 — アクセス、訂正、削除、可搬性、異議、差別されないこと — を
与えている。

Deskhub は個人データを収集も保持もしないため、これらの権利を行使する対象となるデータが
存在しない。もし私たちがあなたのデータを持っていると思う場合は、下記の連絡先に連絡して
ほしい。30 日以内に回答する。

私たちは CCPA が定義する意味で個人情報を「販売」も「共有」もしない。

## 9. 子どものプライバシー

本ソフトウェアは子どもを対象としておらず、上述のとおり、13 歳未満（COPPA）や 16 歳未満
（GDPR）の子どもを含め、誰からもデータを収集しない。

## 10. データの国際移転

なし。本ソフトウェアを通じて、あなたのデータがあなた自身の端末とネットワークを離れる
ことはない。

## 11. 本ポリシーの変更

本ソフトウェアのデータの扱いが変わる場合（たとえば将来のバージョンが任意のクラッシュ
レポートを加える場合など）、本ポリシーはその変更が出荷される**前に**更新され、新しい発効日と
下の変更履歴が付く。最新版は常に次の場所で公開している：
https://github.com/manhpham90vn/Deskhub/blob/main/PRIVACY.md

| バージョン | 日付 | 変更 |
|---|---|---|
| 2.4 | 2026-08-28 | **スマートフォンとタブレットが、ファイルを送るだけでなく受け取るようになった。**受け取ったファイルの行き先も新しい。iOS では写真や動画は写真ライブラリに追加され、初回にシステムの追加専用の写真権限を求める — Deskhub は追加しかできず、既にあるものを読むことも変えることもない — それ以外はアプリの Documents フォルダに置かれ、ファイルアプリから見える。Android では写真は `Pictures/Deskhub`、動画は `Movies/Deskhub`、それ以外は `Download/Deskhub` へ、いずれもシステムのメディアストア経由で入る。どちらも届いたものを通知で知らせる。そのいずれも私たちには届かない。本バージョンは、本ポリシーの旧版が誤って述べていた 2 点も訂正する：Android は端末自身が鳴らす音を取り込むために、システムが*マイク*と表示する権限（`RECORD_AUDIO`）を以前から必要としていた（それがバージョン 2.1 のいう音の共有である。Deskhub は今もマイクを録音しない）。また、デスクトップアプリは 2.3 に述べた *File transfer* のチェック自体を失ったことはなく、失ったのはその裏にある保存された設定だけである。 |
| 2.3 | 2026-08-24 | **ファイルを受け取ることが、保存される設定ではなくなった。**保存されていた *Take files viewers send* の設定は `ui-settings.txt` から削除された：スマートフォンやタブレットはアプリが画面に出ている間はいつでもファイルを受け取り、コンピュータは共有するものの 1 つとして *File transfer* を差し出す。毎回既定でチェックされ、記憶されないので、共有している間だけファイルを受け取ることに変わりはない。届いたファイルの扱いは変わらない：ペアリング済みで入場を許された送信者が必要で、そのマシンが受信ファイルを置く場所に届き、既にあるファイルを上書きすることはなく、送信端末の名前・アドレス・鍵フィンガープリントとともにローカルに記録される。画面の共有は専用のボタンの後ろにある意図的な行為のままで、画面を共有中のコンピュータは、その間ファイル受け取りを止めるのではなく同時に受け取り続ける。 |
| 2.2 | 2026-08-21 | Deskhub が自分の端末どうしで**ファイルを送れる**ようになった。画面を共有しているコンピュータはファイルを受け取ることも申し出られ、それに接続したどの端末もファイルを選んで送れる。Android と iOS ではシステムの写真ピッカーかファイルブラウザから取り、送信中はアプリ自身のキャッシュにコピーが置かれ、その後削除される。ファイルは映像と同じ暗号化トランスポートで、あなたの 2 台の間を直接移動し、私たちにも私たちのサーバにも送られない。受け取るコンピュータは自分で選んだフォルダ — 別途選ばない限りそのユーザーのホームフォルダの `Deskhub`。他の設定とともに `ui-settings.txt` に保存される — に書き、既にあるファイルを上書きせず、各申し出とその判断、結果を、送信端末の名前・アドレス・鍵フィンガープリントとともに自身のローカルなセッションログに書く。ファイルの受け取りは共有する人がチェックしない限りオフで、スマートフォンとタブレットは送るだけ：受け取ることはない。 |
| 2.1 | 2026-08-19 | 画面の共有で、そのコンピュータの**音**も共有できるようになった。取り込まれるのはコンピュータ自身のスピーカーが鳴らしているミックスであって、マイクではない。Deskhub に双方向音声はなく、マイク権限も求めない。音声は圧縮され、映像と同じ暗号化トランスポートで見ている人へ直接送られ、保存されない。流れるのは共有側が *Share this device's sound* をオンにし、**かつ**ビューアが *Play the sound of the device you are watching* をオンにしているときだけで、どちらのスイッチでも止められる。両スイッチとも他の設定とともに `ui-settings.txt` に保存され、既定はオンである。 |
| 2.0 | 2026-08-15 | セッションが暗号化トランスポート（QUIC/TLS）の上を走るようになった — 映像、入力、クリップボード、ターミナルの通信すべて — マシンの入場はペアリングで決まるようになった。あなた自身の端末に新しく保存されるデータは、いずれもアプリのフォルダ内で、私たちに送られることはない：このマシンの身元となる鍵ペア（`host_key.pem`、`host_cert.pem`）、信頼したホストの鍵（`known_hosts`）、このホストに入れたマシン（`paired_devices` — 鍵のフィンガープリント、各自が送ってきた名前、時刻）、秘密でないソルト（`auth_salt`）。パスコードは任意になり、決して送信されない：ペアリングのハンドシェイクが送らずに証明する。 |
| 1.9 | 2026-08-14 | Linux で、デスクトップの画面共有ダイアログでの選択が記憶されるようになった：デスクトップが発行する権限トークンを `portal-restore-token.txt` に保存し、以後の共有はダイアログを飛ばす。トークンはこのマシンのあなた自身のデスクトップセッションでしか働かず、送信されず、共有のたびに置き換えられ、*Choose screens again* を押すかファイルを削除すると消える。 |
| 1.8 | 2026-08-14 | 新しい*スリープ防止*トグル（既定オン）：共有中や視聴中、アプリは OS にマシンをスリープさせない・ディスプレイを消させないよう要求し、セッション終了時にその要求を解除する。保存されるのはオン/オフの選択だけで、同じローカルの設定ファイルに入る。これに関して送信されるものはなく、システムのスリープ設定も変更しない。 |
| 1.7 | 2026-08-13 | クリップボード同期が Android と iOS でも動くようになった。デスクトップと同じトグル、32 KiB の上限、プレーンテキストのみという規則である。OS が制約する：Android 端末は Deskhub が前面にあるときだけ自分のクリップボードを読める（届いたテキストはいつでも適用される）。iOS は Deskhub が新しいコピーを読むときにシステムのペーストプロンプトを出すことがある。ホストになっている iOS 端末はクリップボードを同期しない。ブロードキャストが別プロセスで動くからである。スマートフォンとタブレットにも、どのネットワークアドレスで共有するかというデスクトップの選択が加わり、同じローカル設定ファイルに保存され、どこにも送られない。その選択以外に、どの端末にも新しく保存されるものはない。 |
| 1.6 | 2026-08-13 | デスクトップアプリに任意のクリップボード同期が加わった：トグルをオンにすると、セッション中にコピーしたプレーンテキストが端末間で送られ（他の通信と同じく非暗号、32 KiB まで）、相手のクリップボードに入る。Deskhub がそれを保存することはない。新しくローカルに保存される設定：どのネットワークアドレスで共有するか、OS 起動時に起動、起動時の自動共有、バックグラウンド/トレイモード、そしてクリップボードのトグル自体。OS 起動時に起動をオンにするとプラットフォーム自身の起動エントリ（Linux では autostart ファイル、Windows では *Deskhub* という名のスケジュールタスク、macOS ではログイン項目）が作られ、オフにすると消える。 |
| 1.5 | 2026-08-13 | 各クライアントがデバイス名（接続画面の *Your name* 欄）を設定できるようになった。名前はあなた自身の端末にある既存の設定ファイル `ui-settings.txt` に保存され、接続時にホストへ送られる — 他の通信と同じく非暗号で — ホストがセッション一覧、ステータス行、ログでこのビューアに名前を付けられるようにするためである。ホストは接続中だけ名前をメモリに保持し、保存はしない。欄にはコンピュータや端末自身の名前があらかじめ入っているので、自分で選んだ名前に置き換えない限りその既定値が送信される。 |
| 1.4 | 2026-08-12 | ブロードキャスト拡張がアプリと共有する iOS の状態ファイルに、拡張自身のメモリ使用量（MB）も記録されるようになり、共有画面がそれを表示できるようになった。この値は Deskhub のブロードキャストプロセスだけを表し、端末上の app group コンテナ内に留まり、ブロードキャストが終われば状態ファイルの残りとともに削除される。 |
| 1.3 | 2026-08-12 | Android と iOS の端末が自分の画面を閲覧専用で共有できるようになり、スマートフォンやタブレットの画面をあなたの別の端末へ流せるようになった。これにより各 OS が要求する画面キャプチャ権限（Android ではさらにフォアグラウンドサービスとその通知）が加わり、iOS ではパスコードとポートのためにアプリとブロードキャスト拡張が共有する app group コンテナと、ブロードキャストが動いているかをアプリが表示できるよう拡張がそこに書く短命の状態ファイルが加わった。映像は変わらずあなた自身の端末間だけを移動し、保存されない。 |
| 1.2 | 2026-08-07 | パスコードがすべてのホストで必須になり、空欄のままではなく初回起動時に生成されるようになった。すべてのクライアントが入力できる。共有の設定が Windows に加えて macOS と Linux でも保存され、最近の端末一覧はすべてのプラットフォームで、それぞれアプリ自身のローカルフォルダ内に保存されるようになった。あなたの端末を出る新しいデータはない。 |
| 1.1 | 2026-08-05 | Windows アプリが起動をまたいでデータを保存するようになった：直近 10 件の接続先アドレスの一覧、共有の設定、そのどちらかで使ったパスコード。すべてあなた自身のマシンの `%USERPROFILE%\.deskhub` に留まり、どこへも送信されない。閲覧専用の共有と 5 人のビューア上限を明文化した。 |
| 1.0 | 2026-07-24 | 初版公開。 |

## 12. 連絡先

本ポリシーや Deskhub のプライバシーに関する質問は：

- **メール：** manhpv151090@gmail.com
- **Issues：** https://github.com/manhpham90vn/Deskhub/issues
