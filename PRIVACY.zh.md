[English](PRIVACY.md) · [Tiếng Việt](PRIVACY.vi.md) · **中文** · [日本語](PRIVACY.ja.md)

# Deskhub 隐私政策

_生效日期：2026 年 8 月 28 日 —— 版本 2.4_

> 本文件是 [`PRIVACY.md`](PRIVACY.md) 的译本。若两者有出入，**以英文版为具有法律效力的
> 版本**。

## 1. 引言

本隐私政策说明 **Deskhub**（"本应用"、"我们"）在你使用 Deskhub 移动应用（iOS、Android）
以及 Windows、macOS 和 Linux 的 Deskhub 桌面应用（合称"本软件"）时如何处理信息。

Deskhub 是一款远程桌面应用：它把你其中一台电脑的屏幕推流到你的另一台设备上，并让你用
鼠标、键盘和触摸输入去控制那台电脑。

本软件由一位独立开发者开发和发布：

- **开发者：** Manh Pham
- **联系方式：** manhpv151090@gmail.com
- **项目页面：** https://github.com/manhpham90vn/Deskhub

## 2. 简短版本

**Deskhub 不收集、不存储、不出售、不共享任何个人数据。我们不运营任何服务器，关于你或你
使用情况的数据也绝不会通过本软件到达我们或任何第三方手中。** 没有用户账号、没有分析、
没有崩溃上报、没有广告，本软件里也没有嵌入任何第三方 SDK。

## 3. 本软件会处理的信息

为了工作，本软件必须**完全在你自己的设备上以及设备之间**处理某些数据。这些数据没有一项
会传给开发者或任何第三方。

| 数据 | 用途 | 去向 | 保留时间 |
|---|---|---|---|
| 被共享电脑的屏幕内容（视频帧） | 在你另一台设备上显示那块屏幕 | 直接在你的两台设备之间传输，传输中加密（QUIC/TLS） | 从不存储；只在会话期间存在于内存中 |
| 被共享电脑正在播放的声音（仅当它共享声音且观看者要求时） | 让观看的人听到那台电脑的声音 | 直接在你的两台设备之间以压缩音频传输，传输中加密（QUIC/TLS） | 从不存储；只在会话期间存在于内存中 |
| 鼠标、键盘和触摸输入 | 从你另一台设备控制被共享的电脑 | 直接从观看设备发到被共享的电脑，传输中加密（QUIC/TLS） | 从不存储；注入后即丢弃 |
| 本机的密钥对 —— 首次运行时创建的私钥和自签名证书 | 向它所连接的机器证明本机身份；人看到的是一个指纹（`SHA256:…`） | 写入应用自己文件夹里的 `host_key.pem` 和 `host_cert.pem`；只有公开的那一半（证书）会出示给你连接的机器 | 保留到你删除这些文件为止；删掉它们会让这台机器换一个新身份，认得旧身份的机器会发出警告 |
| 本设备信任过的主机的密钥（指纹、地址、标签、首次/最后出现时间） | 认出已知主机，并在其密钥变更时大声告警 | 写入同一文件夹里的 `known_hosts`；从不传输 | 保留到你删除该文件为止 |
| 与本主机配对的机器 —— 它们的密钥指纹、发来的名字、配对时间和最后出现时间 | 让已配对的机器无需通行码即可重连，并列在 Devices 页面上供你忘记它们 | 写入同一文件夹里的 `paired_devices`；从不传输 | 保留到你在 Devices 页面忘掉那台机器或删除该文件为止 |
| 用于通行码验证器的随机盐值 | 把通行码变成配对握手所校验的值，使通行码本身从不传输 | 写入同一文件夹里的 `auth_salt`；握手期间盐值会发给正在连接的机器（它不是秘密） | 保留到你删除该文件为止 |
| 你输入的地址（IP/主机名） | 连接到另一台机器 | 留在你输入它的那台设备上 | 在本地保留到你改动为止 |
| 你最近连接的 10 个地址、每个的时间，以及你为每个用过的通行码 | 填充*最近的设备*列表，让你一键重连 | 写入你设备上应用自己文件夹里的 `recent-devices.txt` —— Windows 上是 `%USERPROFILE%\.deskhub`，macOS 和 Linux 上是 `~/.deskhub`，iOS 和 Android 上是应用沙盒 | 保留到你连接了 10 个更新的地址，或者你删除该文件为止 |
| 你的共享偏好（帧率、码率、分辨率上限、端口、在哪个网络地址上共享、观看者能否控制本机、剪贴板同步、保持唤醒、随系统启动、自动共享和后台模式这些开关，以及你要求观看者提供的通行码） | 下次打开应用时恢复你的设置 | 写入同一文件夹里的 `ui-settings.txt`。在 iOS 上该文件位于应用与其广播扩展共享的 app group 容器里，好让两半对你的通行码和端口保持一致 | 保留到你改动它们或删除该文件为止 |
| Linux 桌面在你于其屏幕共享对话框中选好显示器后签发的屏幕权限令牌（仅 Linux） | 让之后的共享静默复用你的选择，使对话框只在第一次出现 | 写入同一文件夹里的 `portal-restore-token.txt`；该令牌只对本机上你自己的桌面会话有意义，且从不传输 | 每次共享后替换；按下 *Choose screens again* 或删除该文件时移除 |
| 剪贴板文本（仅当剪贴板同步开关打开且会话进行中时） | 让一台设备上复制的文本能在其他设备上粘贴 | 直接在你的设备之间传输，传输中加密（QUIC/TLS），每次复制上限 32 KiB；只有纯文本，绝无图片或文件 | Deskhub 从不存储；只存在于每台设备正常的系统剪贴板里 |
| 当前是否有广播在运行、连接了多少观看者、广播扩展自身的内存占用（MB），以及上一次启动错误的文本（仅 iOS） | 让应用的共享界面能报告广播扩展的状态 —— iOS 把它作为独立进程运行，占用内存过多就会终止它 | 写入同一个 app group 容器里的 `broadcast-status.txt` | 广播结束时删除 |
| *Your name* 字段里的设备名 —— 在你编辑之前，预填为本电脑或本设备自己的名字（Windows 和 Linux 上是主机名，macOS 上是电脑名称，iOS 上是设备名，Android 上是型号） | 显示在你连接的主机上、本设备地址旁边，好让共享方区分不同的观看者 | 保存在同一文件夹的 `ui-settings.txt` 里，并在你连接时发给主机 —— 传输中加密，但会显示在主机屏幕上并写进它的日志 —— 所以除非你换成自己选的名字，否则这个默认名字就会被传输；清空字段只会恢复默认值，而默认值随后会被保存和发送。机器配对时，主机还会把名字存进它的 `paired_devices` 列表，直到你在那里被忘记 | 默认是电脑或设备的名字；保留到你改动它或删除该文件为止。清空字段是恢复默认值，而不是把名字去掉 |
| 你选择发送给已连接电脑的文件（仅当你挑选它们并按下 Send 时） | 把文件从你的一台设备放到另一台上 | 直接在你的两台设备之间传输，传输中加密（QUIC/TLS）；在手机或平板上会先在应用自己的缓存里暂存一份副本，以便发送时读取 | 文件落在哪里取决于接收方是什么。电脑会写进它为此选定的文件夹 —— 除非另选，否则是该用户主目录下的 `Deskhub` —— 并一直保留到该用户删除它。手机或平板没有这样的文件夹：照片或视频会被加进该设备自己的相册（Android 上是 `Pictures/Deskhub` 和 `Movies/Deskhub`），其他文件都放到系统文件浏览器能看到的地方 —— iOS 上是应用的 Documents 文件夹，Android 上是 `Download/Deskhub` —— 并留在那里直到你删除。在 iOS 上，相册拒收的照片会改放 Documents。发送方手机或平板上的暂存副本，在发送窗口关闭时删除 |
| 每个提出发送的文件的名字、大小和校验和，以及发送设备的名字、地址和密钥指纹 | 让接收电脑能显示正在到达的东西、拒绝它存不下的东西，并让它的主人看到是谁发了什么 | 在你的两台设备之间传输，传输中加密；接收电脑会把这次提出、它的裁决和结果写进自己的会话日志 | 保留在那台电脑的日志文件里，直到你删除 |
| 电脑用于存放收到文件的那个文件夹 | 下次打开应用时恢复这个选择 | 写入应用自己文件夹里的 `ui-settings.txt`；从不传输 | 保留到你改动它或删除该文件为止 |
| 连接统计（码率、丢包、时延） | 自适应流的画质；显示在状态栏上 | 只在你的两台设备之间交换 | 从不存储；会话结束即丢弃 |

### 3.1 设计上就是点对点

所有通信都**直接发生在你自己的两台设备之间**，通过：

- 你的本地网络（Wi-Fi/LAN），或者
- 一个由**你自己**运营或订阅的 VPN（例如 Tailscale），如果你选择用它来做互联网访问。

我们不运营中继服务器、信令服务器或任何其他后端。本软件在技术上没有任何手段把数据发给
开发者。

### 3.2 我们**不**处理的数据

本软件不访问也不处理：你的姓名（上面所述的设备名除外，它默认取你电脑或设备自己的名字）、
电子邮件地址、电话号码、通讯录、位置、照片、文件（你选择推流的那块 PC 屏幕上可见的内容
除外）、麦克风、摄像头、广告标识符，或者超出操作系统运行本应用所需之外的任何设备标识符。

### 3.3 共享手机或平板的屏幕

Android 和 iOS 设备既能看别的机器，也能共享自己的屏幕。这条流是**只读**的：进来的鼠标和
键盘数据包会被丢弃，因为这两个操作系统都不允许普通应用操控设备。被采集的是**整块屏幕**，
包括共享期间出现的任何东西 —— 通知、其他应用、银行应用、你输入的密码。在 Android 上，
系统会为每一次共享弹出自己的录制同意对话框，并在运行期间常驻一条通知；在 iOS 上，系统的
广播指示会一直可见。这两者都是操作系统自己的信号，任何一个都可以随时用来停止共享。和
桌面端一样，视频直接发到你的另一台设备，从不存储，也从不发给我们。

### 3.4 屏幕共享和远程控制的范围

共享推流的是**整块所选显示器**：那台显示器上出现的一切，已连接的观看者都能看到，包括
通知、弹窗，以及你在共享期间打开的任何窗口。（共享单个应用窗口的功能已于 2026-07-27
移除；本软件现在只共享整块显示器。）当你允许远程控制时，观看者的输入会像他们坐在这台 PC
前一样被注入，并且能够到达**共享显示器上可见的任何应用** —— 不再局限于某一个窗口。在
任何主机上，你都可以在设置里彻底关掉远程控制，这会让共享变成只读：抵达的输入会被丢弃而
不是注入。只要允许控制，两道安全机制就始终生效：如果 PC 前的人碰了真实的鼠标或键盘，
远程输入就会暂停（"主机优先"）；连接结束或观看者切走时，远端按着的任何键都会被自动释放。
最多可以有五个观看者同时看一台 PC，但任何时刻只有其中一个在操控鼠标和键盘。

## 4. 应用申请的权限

| 平台 | 权限 | 为什么 |
|---|---|---|
| iOS | 本地网络 | iOS 要求它才能与同一网络上的 PC 收发流量。仅用于推流会话。 |
| iOS | 屏幕录制（广播） | 仅当你从系统广播选择器开始共享本设备屏幕时。iOS 每次都会询问，并全程显示录制指示。 |
| Android | `INTERNET`、网络状态 | 打开到你 PC 的 UDP 连接所必需。仅用于推流会话。 |
| Android | 屏幕采集同意（`MediaProjection`） | 仅当你开始共享本设备屏幕时。Android 每次都会询问；答案无法被记住。 |
| Android | `FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_MEDIA_PROJECTION` | 让共享在应用进入后台或屏幕关闭时继续运行。Android 对屏幕采集的强制要求。 |
| Android | `RECORD_AUDIO` | Android 把它的播放采集 API 放在这个权限后面，而播放 —— 设备自己正在播放的东西 —— 是 Deskhub 唯一采集的内容。共享开始时申请；拒绝的话，共享照常进行，只是没有声音。Deskhub 从不打开麦克风。 |
| Android | `POST_NOTIFICATIONS` | 显示 Android 在屏幕共享运行期间要求的那条持续通知，并在别的设备发来文件时说明到达了什么。不发送任何其他通知。 |
| iOS | 相册，仅添加 | 在别人发来的照片或视频第一次到达本设备时申请，以便把它放进"照片"应用。Deskhub 只能添加：它从不读取、修改或删除你相册里已有的东西。拒绝的话，文件改放应用的 Documents 文件夹。 |
| iOS | 通知 | 在别的设备发来文件时说明到达了什么。不发送任何其他通知。 |

在桌面系统上共享声音不需要它自己的权限：它采集的是电脑自己正在播放的东西，不是麦克风。
Android 是例外，而且只是名义上的：它的播放采集 API 被挡在 `RECORD_AUDIO` 后面，所以要
共享声音的 Android 设备必须持有系统标为*麦克风*的那个权限。Deskhub 不把它用于任何其他
用途。它从不录制麦克风，没有双向音频，在任何其他平台上也不申请麦克风权限。

这些应用不申请任何其他权限。如果将来的版本需要新权限，会在具体场景中申请，并会更新本
政策。

## 5. 分析、广告和第三方

- **分析 / 遥测：** 没有。
- **崩溃上报：** 没有。诊断日志（`[DIAG]`）只存在于你自己的机器上 —— 在应用的控制台
  输出里，以及在 Windows、macOS 和 Linux 上 `~/.deskhub/`（Windows 上是
  `%USERPROFILE%\.deskhub`）下的纯文本文件里。它们绝不会被上传到任何地方；只有你自己
  复制并发送时它们才会离开你的设备，而且你随时可以删掉那个文件夹。
- **广告：** 没有。
- **第三方 SDK：** 没有。本软件只由它自己的源码（可在项目页面获取）和操作系统框架构建
  而成。
- **应用商店：** 这些应用通过 Apple App Store 和 Google Play 分发。Apple 和 Google 可能
  按它们自己的隐私政策收集安装/使用统计；那种收集不在我们的控制之内，我们只能拿到那些
  平台向每个开发者展示的聚合匿名统计。
- **Tailscale 或其他 VPN：** 如果你选择通过 VPN 连接，你的流量按该提供商的隐私政策处理。
  Deskhub 既不要求也不捆绑任何 VPN。

## 6. 安全

- 推流的流量留在你自己的网络或你自己的 VPN 隧道里。当你使用像 Tailscale 这样的 VPN 时，
  设备之间的流量由那个 VPN（WireGuard）做端到端加密。
- Deskhub 会加密它的会话流量 —— 视频、控制、输入、剪贴板和终端数据全都跑在你设备之间的
  QUIC/TLS 上。准入由一次配对握手决定：陌生机器必须证明自己知道主机那个可选的 4 位通行码
  （通行码本身从不传输），或者由主机前的人批准。设备名在传输中是加密的，但会显示在主机
  上，所以别在里面放敏感内容。永远不要把 Deskhub 直接暴露到互联网上。完整的威胁模型 ——
  什么受保护、什么不受保护，以及如何报告漏洞 —— 在
  [`SECURITY.md`](https://github.com/manhpham90vn/Deskhub/blob/main/SECURITY.md) 里。
- 保存在 `recent-devices.txt` 和 `ui-settings.txt` 里的通行码用一个固定密钥做了混淆，
  所以一眼看不出来。那不是加密，也不是为了防住已经能访问你用户账户的人。
- 因为我们不持有关于你的任何数据，所以不存在可能被攻破的开发者侧数据库。

## 7. 数据保留与删除

我们什么都不保留，所以也没有什么需要我们删除。所有会话数据在会话结束时消失。应用里保存的
地址，清空字段或卸载应用即可移除。最近设备列表和已保存的设置 —— 包括任何通行码 —— 通过
删除应用的文件夹（Windows 上 `%USERPROFILE%\.deskhub`，macOS 和 Linux 上 `~/.deskhub`）
即可移除，应用会在下次启动时重新建一个空的；在 iOS 和 Android 上，卸载应用就会移除它们。

别的设备发给你的文件是你的，不是应用的。一旦送达，它们就住在那台电脑选定的文件夹里，或者
在手机、平板上的相册、Documents 或 Downloads 里，卸载 Deskhub 也会把它们原样留在那儿；
要删就在那里删。

## 8. 你的权利（GDPR、CCPA 及类似法律）

诸如欧盟《通用数据保护条例》（GDPR）和《加州消费者隐私法案》（CCPA）之类的法律赋予你对
个人数据的权利 —— 访问、更正、删除、可携带、反对，以及不受歧视。

因为 Deskhub 不收集也不持有任何个人数据，所以没有可供行使这些权利的数据。如果你认为我们
确实持有关于你的数据，请通过下面的地址联系我们，我们会在 30 天内答复。

我们不按 CCPA 定义的方式"出售"或"共享"个人信息。

## 9. 儿童隐私

本软件不面向儿童，而且如上所述，不从任何人那里收集数据，包括 13 岁以下（COPPA）或 16 岁
以下（GDPR）的儿童。

## 10. 跨境数据传输

没有。通过本软件，你的数据从不离开你自己的设备和网络。

## 11. 本政策的变更

如果本软件的数据处理方式将来发生变化（例如某个未来版本加入了可选的崩溃上报），本政策会
在该变化发布**之前**更新，并附上新的生效日期和下面的变更记录。当前版本始终发布在：
https://github.com/manhpham90vn/Deskhub/blob/main/PRIVACY.md

| 版本 | 日期 | 变更 |
|---|---|---|
| 2.4 | 2026-08-28 | **手机和平板现在既能发文件也能收文件**，而文件落在它们哪里是新增内容。在 iOS 上，照片或视频会被加进你的相册，第一次会申请系统的仅添加相册权限 —— Deskhub 只能添加，绝不读取或修改已有内容 —— 其他一切放进应用的 Documents 文件夹，"文件"应用能看到。在 Android 上，照片进 `Pictures/Deskhub`，视频进 `Movies/Deskhub`，其余一切进 `Download/Deskhub`，都通过系统媒体库。两者都会用通知说明到达了什么。这些没有一样会到达我们这里。本版本还更正了本政策早先版本中说错的两件事：Android 一直都需要系统标为*麦克风*的那个权限（`RECORD_AUDIO`）才能采集设备自己播放的内容，也就是版本 2.1 所描述的共享声音 —— Deskhub 仍然从不录制麦克风 —— 以及桌面应用从未失去 2.3 中所述的 *File transfer* 勾选项，失去的只是它背后那个已保存的设置。 |
| 2.3 | 2026-08-24 | **收文件不再是一项已保存的设置。** 存储的 *Take files viewers send* 偏好已从 `ui-settings.txt` 中移除：手机或平板只要应用在屏幕上就收文件，而电脑把 *File transfer* 作为它共享的东西之一，每次默认勾选且从不记住，所以它仍然只在共享期间收文件。文件到达后会发生什么没有变化：仍然需要一个已配对、已放行的发送方，仍然落在那台机器存放接收文件的地方，仍然绝不覆盖已有文件，并且仍然连同发送设备的名字、地址和密钥指纹一起记在本地日志里。共享屏幕仍然是一个由专门按钮触发的有意动作，而共享屏幕的电脑会同时继续收文件，而不是在此期间把它关掉。 |
| 2.2 | 2026-08-21 | Deskhub 现在能在你自己的设备之间**发送文件**。共享屏幕的电脑还可以提出接收文件，任何连接到它的设备都能挑选文件并发送；在 Android 和 iOS 上，文件来自系统相册选择器或系统文件浏览器，发送期间会在应用自己的缓存里暂存一份副本，之后删除。文件通过与画面相同的加密传输，直接在你的两台设备之间传输，绝不发给我们，也不经过我们的任何服务器。接收电脑把它们写进自己选定的一个文件夹 —— 除非另选，否则是该用户主目录下的 `Deskhub`，与你的其他偏好一起保存在 `ui-settings.txt` 里 —— 绝不覆盖已有文件，并把每次提出、它的裁决和结果，连同发送设备的名字、地址和密钥指纹，写进自己本地的会话日志。除非共享方勾选，否则不接收文件，而手机和平板只发送文件：它们从不接收。 |
| 2.1 | 2026-08-19 | 共享屏幕现在也能共享那台电脑的**声音**。采集的是电脑自己扬声器正在播放的混音 —— 绝不是麦克风；Deskhub 没有双向音频，也不申请麦克风权限。音频经过压缩，通过与画面相同的加密传输直接发给观看的人，且从不存储。只有当共享方打开了 *Share this device's sound* **并且**观看者打开了 *Play the sound of the device you are watching* 时它才会传输；任一开关关掉就没有。两个开关都与你的其他偏好一起保存在 `ui-settings.txt` 里，默认都是开的。 |
| 2.0 | 2026-08-15 | 会话现在跑在加密传输（QUIC/TLS）上 —— 视频、输入、剪贴板和终端流量一视同仁 —— 机器通过配对被准入。新存储在你自己设备上的数据，全都在应用的文件夹里，且绝不发给我们：作为本机身份的一对密钥（`host_key.pem`、`host_cert.pem`）、你信任过的主机的密钥（`known_hosts`）、被允许进入这台主机的机器（`paired_devices` —— 密钥指纹、各自发来的名字、时间戳），以及一个非秘密的盐值（`auth_salt`）。通行码现在是可选的，而且从不传输：配对握手在不发送它的情况下证明它。 |
| 1.9 | 2026-08-14 | 在 Linux 上，你在桌面屏幕共享对话框里做的屏幕选择现在会被记住：桌面签发的权限令牌保存在 `portal-restore-token.txt` 里，好让之后的共享跳过对话框。该令牌只对本机上你自己的桌面会话有效，从不传输，每次共享后替换，并在你按下 *Choose screens again* 或删除该文件时移除。 |
| 1.8 | 2026-08-14 | 新增*保持唤醒*开关（默认开）：在你共享或观看期间，应用请求操作系统不要让机器睡眠、不要关闭显示器，并在会话结束时撤销该请求。只有开/关这个选择会被存储，存在同一个本地设置文件里；与它有关的任何东西都不会被传输，也不会改动任何系统睡眠设置。 |
| 1.7 | 2026-08-13 | 剪贴板同步现在在 Android 和 iOS 上也能用，与桌面端有相同的开关、32 KiB 上限和纯文本限制。操作系统对它有限制：Android 设备只有在 Deskhub 处于前台时才能读自己的剪贴板（收到的文本任何时候都会应用）；iOS 在 Deskhub 读取新复制内容时可能弹出系统粘贴提示；正在做主机的 iOS 设备从不同步剪贴板，因为它的广播跑在一个独立进程里。手机和平板也获得了桌面端那个"在哪个网络地址上共享"的选择，保存在同一个本地设置文件里，且从不发往任何地方。除了这个选择之外，任何设备上都没有新增存储的东西。 |
| 1.6 | 2026-08-13 | 桌面应用获得可选的剪贴板同步：打开它的开关后，你在会话期间复制的纯文本会在你的设备之间传输（和其余流量一样未加密，上限 32 KiB），并放进另一台机器的剪贴板；Deskhub 从不存储它。新增本地保存的设置：在哪个网络地址上共享、随系统启动、启动时自动共享、后台/托盘模式，以及剪贴板开关本身。打开随系统启动会创建平台自己的启动项（Linux 上的一个自启动文件、Windows 上名为 *Deskhub* 的计划任务、macOS 上的一个登录项）；关掉它就移除。 |
| 1.5 | 2026-08-13 | 每个客户端现在可以设置一个设备名（连接页面上的 *Your name* 字段）。名字保存在你自己设备上已有的 `ui-settings.txt` 设置文件里，并在你连接时发给主机 —— 和其余流量一样未加密 —— 好让主机在它的会话列表、状态行和日志里标出这个观看者。主机只在你连接期间把名字保存在内存里，从不存储它。这个字段预填为你电脑或设备自己的名字，所以除非你换成自己选的名字，否则那个默认值就会被传输。 |
| 1.4 | 2026-08-12 | 广播扩展与应用共享的那个 iOS 状态文件现在还会记录扩展自身的内存占用（MB），好让共享界面能显示它。这个值只描述 Deskhub 的广播进程，留在你设备上的 app group 容器里，并在广播结束时与状态文件的其余部分一起删除。 |
| 1.3 | 2026-08-12 | Android 和 iOS 设备现在可以共享自己的屏幕（只读），因此手机或平板的屏幕能被推流到你拥有的另一台设备上。这带来了各系统所需的屏幕采集权限（在 Android 上还有一个前台服务及其通知），以及在 iOS 上一个由应用与其广播扩展共享的 app group 容器（用于你的通行码和端口），外加扩展写在那里的一个短命状态文件，好让应用能显示广播是否在运行。视频仍然只在你自己的设备之间传输，且从不存储。 |
| 1.2 | 2026-08-07 | 通行码现在在每台主机上都是必需的，首次启动时生成而不是留空，而且每个客户端都能输入一个。共享设置现在在 macOS 和 Linux 上也会保存（此前只有 Windows），最近设备列表则在每个平台上都保存，各自存在应用自己的本地文件夹里。没有新的数据离开你的设备。 |
| 1.1 | 2026-08-05 | Windows 应用现在会在两次启动之间保存数据：你最近连接的 10 个地址的列表、你的共享设置，以及与两者一起用过的通行码。所有这些都留在你自己机器上的 `%USERPROFILE%\.deskhub` 里；没有一样会被传输到任何地方。记录了只读共享和 5 个观看者的上限。 |
| 1.0 | 2026-07-24 | 首次发布。 |

## 12. 联系方式

关于本政策或 Deskhub 隐私的任何问题：

- **邮件：** manhpv151090@gmail.com
- **Issues：** https://github.com/manhpham90vn/Deskhub/issues
