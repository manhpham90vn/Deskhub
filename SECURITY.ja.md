[English](SECURITY.md) · [Tiếng Việt](SECURITY.vi.md) · [中文](SECURITY.zh.md) · **日本語**

# Deskhub セキュリティポリシー

_最終更新：2026 年 8 月 15 日_

> 本書は [`SECURITY.md`](SECURITY.md) の翻訳である。相違がある場合は**英語版が正典**と
> なる。

## ⚠️ まずここを読むこと

**Deskhub はセッションを暗号化する。セッションが運ぶすべて — 映像、キー入力、マウス、
クリップボード、ターミナルの通信 — は QUIC/TLS の上を流れ、入場はペアリングの
ハンドシェイクで決まる。未知のマシンは、ホストのパスコードを知っていると証明する
（コード自体はネットワークを流れない）か、ホストの前にいる人に承認されるかのどちらかで
なければならない。** 平文のまま残っているのは探索ビーコンだけで、これは秘密を運ばない。
暗号化された接続の外から届くものは、それ以外すべて捨てられる。

どのホストも**閲覧専用**で共有でき（入力は注入されず捨てられる）、新規ペアリングを完全に
切って、既にペアリング済みのマシンだけを入れることもできる。

暗号化されていることと、インターネットに耐えることは別である。ポートは今も探索プローブに
応答し、4 桁のパスコードは依然として小さな秘密であり、最初の対面は依然として信頼の跳躍で
あり、洪水攻撃に抗うものは何もない。Deskhub はあくまで、信頼できるネットワーク向けに
作られている。

だからこの規則は今も生きている：

> **UDP 47777 を決してポート転送しないこと。共有中のマシンをインターネットに直接
> さらさないこと。リモートアクセスには VPN を使い — このプロジェクトが試験しているのは
> [Tailscale](https://tailscale.com) である — `100.x.y.z` のアドレスに接続すること。**

この規則を守るなら、Deskhub は安全に使える。破るなら、自分のマシンをインターネットに
差し出すことになる。

## 脅威モデル

### Deskhub が守るもの

| | |
|---|---|
| 開発者に届くデータ | 何も届かない。サーバもアカウントもテレメトリもサードパーティ SDK もない。[`PRIVACY.ja.md`](PRIVACY.ja.md) を参照。 |
| 通信を読まれること | すべてのセッションは QUIC/TLS の中を通る — 映像フレーム、キー入力、クリップボードのテキスト、ターミナルのバイトは、2 台の間ですべて暗号化される。パケットキャプチャから得られるのは通信量とタイミングであって、内容ではない。ポートに届いた非暗号のパケットは、探索プローブでない限り捨てられる。 |
| リモートの視聴者とマシンを取り合うこと | 「ホストが勝つ」。実物のマウスかキーボードに触れた瞬間、リモート入力は一時停止する（Windows、macOS、Linux のホストいずれも）。 |
| キーが押されたままになること | セッションが終わるか視聴者が離れたとき、リモート側が押しているキーは自動的に離される。 |
| 招いていない他人が接続してくること | 入場はペアリングのハンドシェイクである。未知のマシンは SPAKE2 でホストのパスコードを証明しなければならない — コードは流れないので盗聴者は持ち帰って解くものを得られず、1 接続につき試行は 1 回だけ — あるいはパスコード未設定なら、ホストの前にいる人が *このマシンを入れますか？* に答えるのを待つ。3 回間違えるとペアリングが 30 秒ロックされる。入場が済んだマシンはペアリング済みとなり、暗号鍵で認識され、ホストの Devices 画面に並び、そこで取り消せる。探索ビーコンはもう当たったコードを教えない — 見知らぬ相手のプローブには中身が何であれ空の一覧が返るので、かつてのブルートフォース用オラクルは消えた。 |
| 2 回目以降の中間者 | どのマシンにも鍵がある。クライアントはペアリングした各ホストの鍵を覚えており、鍵が変わったら、ユーザーが明示的に受け入れるまで再接続を拒む。パスコードの証明はクライアントが実際に見たホスト鍵に紐づくので、中継された証明は検証を通らない。 |
| 視聴者どうしのマウスの取り合い | 1 台のホストを最大 5 人が見られるが、入力を握れるのは 1 人だけ。先に参加したほうが勝ち、後から来た視聴者の入力は、先の視聴者が 1 秒アイドルになるまで捨てられる。6 人目は `Busy` で拒否される。 |
| 画面だけ見せたい相手 | 閲覧専用の共有はどのホストにもあり、何かが注入される前にホスト側で入力パケットを落とす — クライアントに行儀よくしてくれと頼んで実現しているのではない。Android と iOS のホストは無条件に閲覧専用である。 |
| うっかり共有したままの端末 | 最後の砦は Deskhub ではなく OS である。Android は常設の通知を出し続け、共有のたびに毎回録画の同意を取り直す。iOS はブロードキャストのインジケータを出し続ける。どちらもアプリを開かずに共有を止められる。 |
| ペアリング済みのマシンが自分の端末にファイルを書くこと | ファイルを送れるのは入場済みのマシンだけで、しかも受け取る側がファイル転送を提供している間だけである。届いたものは受け取る側が選んだフォルダの外に出られない。ワイヤ上の名前は最後のパス要素まで切り詰められ、ファイルを開く前に区切り文字、制御バイト、ファイルシステムが拒む文字、予約デバイス名が除去される。各ファイルは `.deskhub-part` という名前で書かれ、CRC-32 が一致して丸ごと届いたときにだけ改名される。既にある名前は上書きではなく番号が付く。1 バッチは 32 ファイル、1 ファイル 8 GiB、合計 32 GiB が上限。スマートフォンやタブレットでも、写真ライブラリやダウンロードフォルダに届く前に同じ除去処理が走る。 |
| 不正なパケット | すべてのフィールドは読む前に境界検査される。パーサはユニットテストで覆われ、CI で AddressSanitizer、UndefinedBehaviorSanitizer、ThreadSanitizer の下を走り、libFuzzer で毎晩ファジングされる — ワイヤフォーマット、H.264 パース、パケット再組み立て、ターミナルのバイト列、UI テキスト、ホストとビューアのセッション状態機械を覆う 7 つのターゲット。ファジングで見つかったクラッシュはリポジトリ内に回帰テストとして残り、新しいカバレッジはシードコーパスに畳み込まれる。 |

### Deskhub が守**らない**もの

これは正直な一覧である。以下のどれも今日は解決していない：

- **最初の対面は信頼の跳躍である。** ペアリングが止められるのは*後から*現れる中間者で
  ある — 鍵はピン留めされ、変化は声高に拒まれる。ごく最初の接触の時点で既に中間にいる
  相手は止められない。パスコードを設定していなければ、クライアントが到達した相手がその
  ままペアリング相手になり、パスコードが上げる敷居も 4 桁の秘密の分だけである。それが
  重要なら、別経路でフィンガープリントを突き合わせること。
- **トラフィック解析は依然として効く。** 暗号化が隠すのは中身であって存在ではない。
  観測者にはセッションが動いていること、映像がどれだけ流れているか、いつタイプしているかが
  見える。
- **レート制限も DoS 耐性もない。** ポートを溢れさせればセッションは乱れる。パスコード
  なしのホストなら、承認プロンプトを何度も出させることもできる。
- **探索ビーコンは今も誰にでも答える。** `LIST_SOURCES` プローブや `PING` には、どの
  送信元アドレスからでも返答がある — 見知らぬ相手には空の一覧が返り、どのプローブも
  もはやパスコードを確かめられないが、マシンはスキャンで発見でき、ポートは小さな UDP
  リフレクタとして使える。例外がひとつ。現在暗号化接続を保持している送信元アドレスには
  平文で返答しない — 一度自分を証明したマシンの発言はすべて暗号化されて届かねばならず、
  偽造した平文の `SOURCE_LIST` や `PONG` で接続中のピアになりすますことはできない。
- **デバイス名は表示され、記録される。** 視聴者が送る *あなたの名前* は転送中こそ暗号化
  されるが、ホストの画面に表示され、ホストのログに書かれ、ホストのペアリング済み一覧に
  保存される。既定値はマシン自身のホスト名 — 多くの場合、持ち主の実名である。ニックネームを
  使い、機微なものは絶対に入れないこと。欄を空にしても名前の送信は止まらず、既定値に
  戻るだけである。
- **視聴者の枠は 5 秒の沈黙で自動的に空く。** 自分のビューアが落ちると枠が再び開き、
  次に届いた `Hello` がそれを取る — 送ったのが誰であれ、パスコードだけがその条件である。
- **共有はディスプレイ全体をさらす。** 1 つのウィンドウではない。そのモニタ上のあらゆる
  通知、ポップアップ、ウィンドウである。[`PRIVACY.ja.md` §3.4](PRIVACY.ja.md) を参照。
- **スマートフォンやタブレットのホストは端末まるごとをさらす。** Android と iOS も
  ホストになれるが、流すのは画面全体である — 銀行アプリ、ワンタイムコード、メッセージ、
  共有中に打つすべてのパスワード。ストリームは他のセッションと同様に暗号化されるが、
  入れた視聴者全員がそのすべてを見る。モバイルのホストは常に閲覧専用なので遠隔操作の
  リスクは消えるが、露出のリスクはひとつも消えない。

## どこで動かすなら安全か

✅ **安全**

- すべての機器を自分が掌握している家庭内・個人の LAN。
- 自分の端末だけが参加している Tailscale の tailnet（または他の WireGuard/VPN
  トンネル）。VPN は暗号化の層をもう 1 枚足し、そもそも見知らぬ相手をポートに到達させない。
- *クライアント*にしかならないマシン（スマートフォン、タブレット、画面を共有しない
  ノート PC）。クライアントは着信セッションを受け付けない。

❌ **危険 — やってはいけない**

- ルータで UDP 47777 をポート転送する、共有中のマシンを DMZ に置く。
- カフェ、ホテル、空港、学内、コワーキング、カンファレンスの Wi-Fi で画面を共有する。
- 他のすべての機器を信頼できていないオフィスやシェアハウスの LAN で共有する。
- ゲスト機器、自分が設定していない IoT 機器、管理していない同居人のマシンがある
  ネットワーク全般。
- クラウド VM の公開インターフェースや公開トンネルサービス経由でポートをさらす。

既定ではソケットはすべてのインターフェース（`INADDR_ANY`）にバインドするので、マシンが
接続しているあらゆるネットワークから到達できる — 繋がっているのを忘れていたものも含めて。
**Share on network** の設定はこれを狭める。マシンのアドレスをひとつ選べばホストはその
インターフェースだけにバインドし、他のネットワークのマシンはポートに届くことすらできない。
注意が 2 つ。共有を始めるときに選んだアドレスが既に無ければ（ケーブルが抜けた、DHCP が
別のアドレスを配った）、Deskhub は全インターフェースに戻り、その旨を共有ステータスに
表示する — これに頼るならバナーを確認すること。もうひとつ、ひとつのインターフェースに
バインドすると同じマシン上のループバック（`127.0.0.1`）のビューアも遮断される。Windows
ではアプリは起動した瞬間から昇格して動き（昇格ウィンドウに入力を注入できるよう、一度だけ
尋ねる）、共有時にファイアウォール規則を開けてくれる — その規則はすべてのプロファイルで
アプリ全体を対象にするので、バインドを狭めてもファイアウォールは狭まらない。その利便性
こそが、上の規則を重要にしている。

## 同じネットワークにいる攻撃者にできること

画面を共有しているマシンと同じ LAN に誰かがいて、Deskhub が動いているなら、その人は：

1. UDP 47777 をスキャンして見つけられる。ペアリングしていないマシンのプローブには空の
   一覧が返るが、マシンは応答するので、存在は明かしてしまう。
2. 入ろうと試みられる。もうワイヤからパスコードを読むことはできない — 決して流れない
   からである。残るのはオンラインで当てること（1 接続 1 回、3 回間違えるとペアリングが
   30 秒ロック）か、パスコードなしのホストで、ホストの前の人が承認プロンプトの
   **Allow** を押すのを期待することである。
3. 入らずに通信を眺められる — そして分かるのは量とタイミングだけである。セッションの
   内容は映像を含めて暗号化されており、キャプチャからは画面もキー入力も再現できない。
4. ポートを溢れさせてセッションを妨害できる。マシンに到達できる攻撃者にレート制限を
   かけるものは何もない。

「ホストが勝つ」振る舞いが悪さを抑えるのは、マシンの*前に座っている*間だけである。
離れている間、つまり本当に重要なときには何もしない。

## 堅牢化チェックリスト

いまの Deskhub をこのまま使い続けたいなら、これらはやる価値がある：

- [ ] 両方のマシンで Tailscale を動かし、`100.x.y.z` のアドレス経由でのみ接続する。
- [ ] ルータに UDP 47777 のポート転送や UPnP マッピングが**ない**ことを確認する。
- [ ] マシンをどう入れるか決める：Settings で 4 桁のパスコードを設定するか、空のままに
      して承認プロンプトに自分で答える。Devices 画面をときどき見直し、見覚えのなくなった
      マシンは忘れさせる。見せるだけでよいときは *Viewers can control this machine* の
      チェックを外す。
- [ ] 積極的に使っていないときは Deskhub を終了する。常駐サービスではない — 閉じれば
      穴も閉じる。
- [ ] Linux で `ufw` を使うなら、広く開けずに範囲を絞る：
      `sudo ufw allow 47777/udp` ではなく
      `sudo ufw allow from 100.64.0.0/10 to any port 47777 proto udp`。
- [ ] 別のネットワークへ持ち歩くノート PC で共有を出しっぱなしにしない。
- [ ] 席を立つときはマシンをロックし、放置されたセッションを黙って乗っ取られないように
      する。
- [ ] `deskhub-cli` では、パスコードをコマンド自体に書かないこと。`--passcode 0417` は
      `ps` や `/proc/*/cmdline` を通じてマシン上のすべてのプロセスから見え、シェルの
      履歴にも残る。標準入力から読む `--passcode -`、自分しか読めないファイルから読む
      `--passcode @FILE`、あるいは環境変数 `DESKHUB_PASSCODE` を使うこと。

## ローカルに残るもの

診断ログは Windows、macOS、Linux のいずれでも `~/.deskhub/`（Windows では
`%USERPROFILE%\.deskhub`）の下に平文で書かれる。中身は接続の統計とピアのアドレスで
あって、画面の内容やキー入力ではない。

デスクトップアプリと `deskhub-cli` はこれらのファイルを共有する。同じフォルダにはさらに
`ui-settings.txt`（fps、ビットレート、解像度上限、ポート、閲覧専用とペアリングのスイッチ、
設定したならホストのパスコード、ホストに表示されるデバイス名）、`recent-devices.txt`
（最近接続した 10 件のアドレス、その時刻、それぞれに使ったパスコード）、`host_key.pem` +
`host_cert.pem`（このマシンの秘密鍵と自己署名証明書 — フィンガープリントの背後にある
アイデンティティ。鍵ファイルをコピーした者はこのマシンになりすませる）、`known_hosts`
（このマシンが信頼したホストの鍵）、`paired_devices`（このホストに入れたマシンの鍵、名前、
時刻）、`auth_salt`（パスコード検証子のための秘密でないソルト）、そして Linux では
`portal-restore-token.txt`（選んだ画面に対するデスクトップ自身のトークン。自分のデスクトップ
セッションにしか意味がなく、送信されることはない）が置かれる。モバイルアプリは設定を自分の
サンドボックス内に — iOS ではアプリグループのコンテナに — 保持する。保存されたパスコードは
固定の XOR 鍵で難読化してあり、画面に出さず、うっかりファイルを `type` しても読めないように
している — **これは暗号化ではない**。ソースとファイルを持つ者なら数秒で復元できる。あの
フォルダは、自分の権限で動くものなら何でも読めるものとして扱うこと。

他のマシンが送ってきたファイルはそのフォルダの外、受け取る側が選んだディレクトリに置かれる
（別途選ばない限りユーザーのホームフォルダの `Deskhub`。`transfer_dir` に保存される）。
スマートフォンやタブレットでは端末の写真ライブラリか Documents / Downloads フォルダに入り、
アプリを削除しても残る。そこに届いたものは、ペアリング済みのマシンが自分の端末に置いた
ファイルとして扱うこと。

これらを何かがアップロードすることはない。フォルダはいつでも削除してよい。

## 予定している緩和策

実装したい順に：

1. **パスコードとホスト鍵を** ファイルではなく **OS のキーチェーンに保存する**。
2. **探索ビーコンを黙らせる**。求められていないプローブに空の一覧を返すのではなく、
   そもそも応答しないようにする。

この一覧の前回改訂以降に出荷済み：セッション全体 — 映像、入力、クリップボード、
ターミナルすべて — の暗号化トランスポート（QUIC/TLS）と、探索プローブ以外の非暗号到着の
破棄。パスコードが決して流れず、収集もオフラインでの総当たりもできない SPAKE2 ペアリング。
ホスト側の承認プロンプト。取り消しのできるペアリング済みマシン一覧。マシン鍵と、
クライアント側の鍵変更警告。そしてパスコード誤入力に対する 3 回 / 30 秒のロックアウト。

この一覧は意思の表明であって、予定表ではない。Deskhub は 1 人が空き時間に保守している。
現在の状態を、計画ではなく状態として受け取ってほしい。

## 脆弱性の報告

セキュリティの問題は**非公開で**報告してほしい — 公開の GitHub issue にはしないこと。

- **メール：** manhpv151090@gmail.com — 件名に `[Deskhub security]` と入れること。
- **または：** GitHub で
  [非公開のセキュリティアドバイザリ](https://github.com/manhpham90vn/Deskhub/security/advisories/new)
  を開く。

何を動かしていたか（OS、タイトルバーか [`VERSION`](VERSION) にある Deskhub のバージョン）、
何をしたか、何が起きたかを書いてほしい。概念実証があると大いに助かる。

**見込み：** 7 日以内に受領の連絡、30 日以内に評価。1 人の開発者が空き時間でやっている
プロジェクトなので、時間軸には辛抱してほしい — どちらにせよ率直な回答は返す。修正が
出荷されれば、望まない場合を除いてリリースノートに名前を載せる。

バグバウンティはない。支払いは一切ない。

**上に既に書かれていることは脆弱性ではない。** 上に挙げた限界 — 最初の対面の信頼の跳躍、
トラフィック解析、プローブに答えるビーコン、DoS 耐性の欠如 — は既知であり列挙済みで、
それを言い直す報告は新しいことを何も伝えない。*報告する価値があるのは*：不正なパケットから
到達できるメモリ破壊やクラッシュ、文書化された脅威モデルから抜け出す方法、データをマシンの
外へ漏らすもの、あるいは緩和策が出荷された後にその緩和策自体にある欠陥である。

## サポート対象のバージョン

サポートするのは [Releases ページ](https://github.com/manhpham90vn/Deskhub/releases)の
最新リリースだけである。修正は新しいリリースで出す。古いバージョンへのバックポートはない。

## 適用範囲

本ポリシーは、本リポジトリの Deskhub のソースと、Releases ページ、TestFlight、
Google Play で公開されるバイナリを対象とする。Tailscale、あなたの OS、ルータ、その他
併用しているソフトウェアは対象外である。
