[English](SECURITY.md) · [Tiếng Việt](SECURITY.vi.md) · **中文** · [日本語](SECURITY.ja.md)

# Deskhub 安全策略

_最后更新：2026 年 8 月 15 日_

> 本文件是 [`SECURITY.md`](SECURITY.md) 的译本。若两者有出入，**以英文版为准**。

## ⚠️ 请先读这一段

**Deskhub 会加密它的会话。一次会话承载的一切 —— 画面、按键、鼠标、剪贴板和终端流量 ——
都跑在 QUIC/TLS 上，而准入由一次配对握手决定：陌生机器要么证明自己知道主机的通行码
（通行码本身从不经过网络），要么由主机前的人批准。** 唯一还留在明文里的是发现信标，
它不携带任何秘密；其余在加密连接之外抵达的东西一律丢弃。

每台主机还可以**只读共享**（输入被丢弃而不是注入），也可以完全关闭新配对，只让已配对的
机器进来。

加密不等于能扛住互联网：端口仍然会回应发现探测，4 位通行码仍然是个小秘密，第一次见面
仍然是一次信任的跳跃，而且这里没有任何东西能抵抗洪水攻击。Deskhub 依然是为你信任的网络
而造的。

所以那条规矩依旧成立：

> **永远不要把 UDP 47777 做端口转发。永远不要把正在共享的机器直接暴露到互联网上。要远程
> 访问就用 VPN —— 本项目对着 [Tailscale](https://tailscale.com) 做测试 —— 并连接
> `100.x.y.z` 地址。**

守住这条规矩，Deskhub 用起来是安全的。破了它，你就是把自己的机器交给了互联网。

## 威胁模型

### Deskhub 防得住什么

| | |
|---|---|
| 数据流向开发者 | 一点都没有。没有服务器、没有账号、没有遥测、没有第三方 SDK。见 [`PRIVACY.zh.md`](PRIVACY.zh.md)。 |
| 有人读你的流量 | 每个会话都跑在 QUIC/TLS 里 —— 视频帧、按键、剪贴板文本和终端字节，在两台机器之间全部加密。抓包只能得到流量大小和时间，得不到内容。抵达该端口的未加密包一律丢弃，除非它们是发现探测。 |
| 远端观看者跟你抢机器 | "主机优先"：你一碰真实的鼠标或键盘，远端输入就被暂停（Windows、macOS 和 Linux 主机都一样）。 |
| 按键卡住不放 | 会话结束或观看者切走时，远端还按着的任何键都会被自动释放。 |
| 陌生人不请自来 | 准入是一次配对握手。陌生机器必须通过 SPAKE2 证明知道主机的通行码 —— 通行码从不上网，窃听者拿不到任何可以带回去破解的东西，而且每条连接只允许猜一次 —— 或者在没有设通行码时，等主机前的人回答*让这台机器进来吗？*。猜错三次会把配对锁上 30 秒。一旦被放进来，机器就算配对了：以它的密钥被识别，列在主机的 Devices 页面上，也能在那里撤销。发现信标不再确认猜中的通行码 —— 不管探测里写了什么，陌生人得到的都是一份空列表，所以过去那个暴力破解的谕示已经没有了。 |
| 后续连接中的中间人 | 每台机器都有一个密钥。客户端会记住自己配对过的每台主机的密钥，只要密钥变了就拒绝重连，直到用户明确接受。通行码证明与客户端实际看到的那个主机密钥绑定，所以被转发的证明验不过去。 |
| 观看者之间抢鼠标 | 最多 5 个观看者可以同时看一台主机，但只有一个能操控输入：先加入的那个赢，后来者的输入会被丢弃，直到先来的那个空闲满一秒。第 6 个观看者会被以 `Busy` 拒绝。 |
| 你只想让对方看画面 | 只读共享在每台主机上都有，它在主机侧、任何注入发生之前就把输入包丢掉 —— 而不是靠请求客户端守规矩来实现。Android 和 iOS 主机无条件只读。 |
| 手机被落在那儿一直共享 | 兜底的是操作系统而不是 Deskhub：Android 会常驻一条通知，并且每一次共享都重新征求录制同意，iOS 会一直显示广播指示。两者都可以在不打开应用的情况下停止共享。 |
| 已配对的机器往你这里写文件 | 只有被放进来的机器才能发文件，而且只在接收方正在提供文件传输时才行。送来的东西跑不出接收机器选定的那个文件夹：线上传来的名字会被截到最后一个路径元素，并在打开任何文件之前清洗掉分隔符、控制字节、文件系统拒绝的字符和保留设备名；每个文件先以 `.deskhub-part` 名字写入，只有在完整抵达且 CRC-32 对得上之后才改名；已经存在的同名文件会被加上编号，而不是覆盖任何东西。一批最多 32 个文件、单文件 8 GiB、总量 32 GiB。在手机或平板上，任何东西进入相册或下载文件夹之前，同样会跑一遍这套清洗。 |
| 畸形的数据包 | 每个字段在读取之前都做边界检查。解析器有单元测试覆盖，在 CI 里跑 AddressSanitizer、UndefinedBehaviorSanitizer 和 ThreadSanitizer，并且每晚用 libFuzzer 做 fuzz —— 七个目标覆盖线格式、H.264 解析、包重组、终端字节流、界面文本，以及主机端与观看端的会话状态机。fuzz 找到的崩溃会作为回归测试留在仓库里，新覆盖到的输入会并回种子语料。 |

### Deskhub **防不住**什么

这是一份诚实的清单。下面没有一条在今天是已解决的：

- **第一次见面是一次信任的跳跃。** 配对能挡住*后来*才出现的中间人 —— 密钥被固定，一旦
  改变就会被大声拒绝。它挡不住在第一次接触时就已经在中间的那个：没设通行码时，客户端
  连上谁就跟谁配对，而通行码能提高的门槛，也就只有一个 4 位秘密那么高。如果这对你很重要，
  请通过别的渠道核对指纹。
- **流量分析依然有效。** 加密隐藏的是内容，不是存在：观察者能看出会话正在进行、有多少
  视频在流动，以及你什么时候在打字。
- **没有速率限制，也不抗 DoS。** 冲垮那个端口就能打断会话；没设通行码的主机还能被反复
  弹出审批提示。
- **发现信标仍然会回应任何人。** 一个 `LIST_SOURCES` 探测或一个 `PING` 会从任意源地址
  得到回复 —— 陌生人拿到的是空列表，而且任何探测都不能再确认通行码，但这台机器仍然能被
  扫描发现，端口仍然可以当作一个小型 UDP 反射器。有一个例外：当前持有加密连接的源地址
  绝不会收到明文回复 —— 一台机器一旦证明了自己，它说的一切就必须加密送达，所以伪造的
  明文 `SOURCE_LIST` 或 `PONG` 无法冒充一个已连接的对端。
- **设备名会被显示和记录。** 观看者发送的*你的名字*现在在传输中是加密的，但它仍然会显示
  在主机屏幕上、写进主机的日志、存进主机的已配对设备列表。它默认取机器自己的主机名 ——
  往往就是机主的真名。请用个昵称；千万别在里面放敏感内容。清空这个字段不会让名字不被
  发送，只会恢复默认值。
- **观看者的名额在静默 5 秒后会自行释放。** 如果你的观看端掉线，它的名额会重新开放，
  下一个到达的 `Hello` 就会占走 —— 不管是谁发的，只受通行码约束。
- **共享暴露的是整块显示器。** 不是某一个窗口：那块显示器上的每一条通知、每一个弹窗、
  每一个窗口。见 [`PRIVACY.zh.md` 第 3.4 节](PRIVACY.zh.md)。
- **手机或平板做主机时暴露的是整台手机。** Android 和 iOS 也能做主机，它们推流出去的
  是整个屏幕 —— 银行应用、一次性验证码、消息，以及你在共享期间输入的每一个密码。这条流
  和别的会话一样是加密的，但你放进来的每个观看者都会看到全部内容。移动端主机始终只读，
  这消除了远程控制的风险，但一点也没有消除暴露的风险。

## 在哪里跑是安全的

✅ **安全**

- 家里或个人的 LAN，上面每台设备都由你掌控。
- 只有你自己设备加入的 Tailscale tailnet（或别的 WireGuard/VPN 隧道）。VPN 多加了一层
  加密，也让陌生人根本够不到那个端口。
- 只做*客户端*的机器（手机、平板、从不共享屏幕的笔记本）。客户端不接受任何入站会话。

❌ **不安全 —— 别这么干**

- 在路由器上把 UDP 47777 做端口转发，或者把正在共享的机器放进 DMZ。
- 在咖啡馆、酒店、机场、校园、共享办公或会议的 Wi-Fi 上共享屏幕。
- 在你并不信任其他每台设备的办公室或合租房 LAN 上共享。
- 任何有访客设备、你没配置过的 IoT 设备，或者你管不着的室友机器的网络。
- 通过云虚拟机的公网接口或公共隧道服务暴露那个端口。

默认情况下 socket 绑定到所有接口（`INADDR_ANY`），所以这台机器接入的每一个网络上都能
够到它 —— 包括你忘了自己还连着的那个。**Share on network** 设置可以收窄这一点：选中
机器的某一个地址，主机就只绑定那个接口，其他网络上的机器连端口都够不着。两点提醒：如果
开始共享时所选地址已经不存在了（网线拔了、DHCP 给了新地址），Deskhub 会回退到所有接口
并在共享状态里说明 —— 如果你依赖这一点，请看那条横幅；另外，绑定单个接口也会挡住同一台
机器上的回环（`127.0.0.1`）观看端。在 Windows 上，应用从启动那一刻起就以提权方式运行
（它只问一次，为的是能把输入注入到提权窗口），并在你共享时替你打开防火墙规则 —— 这条
规则覆盖整个应用、覆盖所有配置文件，所以收窄绑定并不会收窄防火墙；正是这份便利，让上面
那条规矩变得重要。

## 同一网络上的攻击者能做什么

如果有人和一台正在共享屏幕的机器在同一个 LAN 上，而 Deskhub 正在运行，他们可以：

1. 通过扫描 UDP 47777 发现它。未配对机器的探测得到的是空列表，但机器仍然会应答，所以
   它仍然暴露了自己。
2. 试着进来。他们已经无法从线上读到通行码 —— 通行码从不传输。剩下的办法是在线猜
   （每条连接一次，猜错三次锁定配对 30 秒），或者在没设通行码的主机上，指望主机前的人
   在审批提示上点 **Allow**。
3. 在进不来的情况下看流量 —— 而只能学到流量大小和时间。会话内容（包括视频）是加密的；
   抓包已经不能重建屏幕或按键了。
4. 冲垮端口、打断会话。对一个够得到这台机器的攻击者，没有任何速率限制。

"主机优先"的行为只在你*坐在*机器前时限制捣乱。你人不在的时候它什么也做不了 ——
而那恰恰是要紧的时候。

## 加固清单

如果你打算就按现在这个样子继续用 Deskhub，这些值得做：

- [ ] 在两台机器上都跑 Tailscale，并且只通过 `100.x.y.z` 地址连接。
- [ ] 确认你的路由器上**没有**针对 UDP 47777 的端口转发或 UPnP 映射。
- [ ] 决定机器怎么进来：在 Settings 里设一个 4 位通行码，或者留空、自己回答审批提示。
      时不时看一眼 Devices 页面，把不再认识的机器忘掉。只需要别人看的时候，就把
      *Viewers can control this machine* 的勾去掉。
- [ ] 不主动使用时就退出 Deskhub。它不作为后台服务运行 —— 关掉它就等于堵上那个口子。
- [ ] 在 Linux 上，如果你用 `ufw`，请限定规则范围而不是全开：
      `sudo ufw allow from 100.64.0.0/10 to any port 47777 proto udp`，而不是
      `sudo ufw allow 47777/udp`。
- [ ] 不要让共享在一台你会带到别的网络上去的笔记本上一直开着。
- [ ] 离开时锁上机器，免得无人看管的会话被悄悄接管。
- [ ] 用 `deskhub-cli` 时，不要把通行码写进命令本身。`--passcode 0417` 会通过 `ps` 和
      `/proc/*/cmdline` 暴露给机器上的每一个进程，而且会留在你的 shell 历史里。请用
      `--passcode -` 从标准输入读，用 `--passcode @FILE` 从只有你能读的文件里读，或者在
      环境里设 `DESKHUB_PASSCODE`。

## 本地留下的东西

诊断日志以明文写在 `~/.deskhub/`（Windows 上是 `%USERPROFILE%\.deskhub`）下，Windows、
macOS 和 Linux 都是如此。里面是连接统计和对端地址，不含屏幕内容或按键。

桌面应用和 `deskhub-cli` 共用这些文件。那个文件夹里还有更多东西：`ui-settings.txt`
（fps、码率、分辨率上限、端口、只读与配对开关、你设的主机通行码（如果设了），以及给主机
看的设备名）、`recent-devices.txt`（你最近连过的 10 个地址、时间，以及各自用的通行码）、
`host_key.pem` + `host_cert.pem`（本机的私钥和自签名证书 —— 指纹背后的身份；任何人拷走
这个密钥文件就能冒充这台机器）、`known_hosts`（本机信任过的主机的密钥）、
`paired_devices`（被允许进入这台主机的机器的密钥、名字和时间戳）、`auth_salt`（用于通行码
验证器的非秘密盐值），以及在 Linux 上的 `portal-restore-token.txt`（桌面自己为你选中的
那些屏幕生成的令牌，只对你的桌面会话有意义，且从不传输）。移动端应用把设置保存在自己的
沙盒里 —— iOS 上是 app group 容器。保存下来的通行码用一个固定的 XOR 密钥做了混淆，这能
让它们不出现在屏幕上、不在随手 `type` 一下文件时露出来 —— **但那不是加密**，任何拿到源码
和这个文件的人几秒钟就能还原。请把那个文件夹当作以你的身份运行的任何东西都能读的东西。

别的机器发来的文件落在那个文件夹之外，落在接收机器为它们选定的目录里（默认是用户主目录
下的 `Deskhub`，除非另选，保存在 `transfer_dir` 里）。在手机或平板上，它们最终会进相册或
Documents / Downloads 文件夹，卸载应用后依然存在。请把送到那里的任何东西，都当作一台已
配对机器放到你设备上的文件来对待。

这些东西没有任何一样会被上传；你随时可以删掉那个文件夹。

## 计划中的缓解措施

按打算落地的顺序跟踪：

1. **把通行码和主机密钥存进操作系统的钥匙串**，而不是文件里。
2. **让发现信标闭嘴**，对未经请求的探测干脆不回应，而不是回一个空列表。

自上一版清单以来已经发布的：整个会话的加密传输（QUIC/TLS）—— 视频、输入、剪贴板和终端
一视同仁 —— 未加密抵达的一律丢弃，除非是发现探测；SPAKE2 配对，使通行码从不传输，也无法
被采集或离线暴力破解；主机上的审批提示；带撤销功能的已配对机器列表；机器密钥以及客户端
上的密钥变更警告；还有针对通行码猜错的三振 / 30 秒锁定。

这份清单是意图声明，不是排期表。Deskhub 由一个人在业余时间维护。请把当前状态当作状态，
而不是当作计划。

## 报告漏洞

请**私下**报告安全问题 —— 不要开成公开的 GitHub issue。

- **邮件：** manhpv151090@gmail.com —— 主题里请写 `[Deskhub security]`。
- **或者：** 在 GitHub 上开一个
  [私密安全公告](https://github.com/manhpham90vn/Deskhub/security/advisories/new)。

请写明你跑的是什么（操作系统、标题栏或 [`VERSION`](VERSION) 里的 Deskhub 版本）、你做了
什么、发生了什么。有一个概念验证会帮上大忙。

**可以期待什么：** 7 天内收到确认，30 天内收到评估。这是一个开发者用业余时间做的项目，
所以请对时间线多些耐心 —— 但无论结论如何，你都会得到一个直截了当的答复。如果修复发布了，
除非你不希望，否则你会被写进发布说明的致谢里。

没有漏洞赏金；不支付任何费用。

**上面已经写明的东西不算漏洞。** 上面列出的那些局限 —— 第一次见面的信任跳跃、流量分析、
信标会回应探测、缺乏抗 DoS 能力 —— 都是已知并已列出的；一份只是把其中之一重述一遍的报告
提供不了新信息。*值得*报告的是：畸形数据包能触达的内存破坏或崩溃、跳出已记录威胁模型的
办法、任何把数据泄漏出这台机器的东西，或者某个缓解措施发布之后它本身的缺陷。

## 受支持的版本

只支持 [Releases 页面](https://github.com/manhpham90vn/Deskhub/releases)上最新的那个
版本。修复随新版本发布；不会回移植到旧版本。

## 适用范围

本策略覆盖本仓库中的 Deskhub 源码，以及发布在 Releases 页面、TestFlight 和 Google Play
上的二进制。它不覆盖 Tailscale、你的操作系统、你的路由器，或者你与它一起运行的任何其他
软件。
